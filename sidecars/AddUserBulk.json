{
  "override_confidence": "high",
  "_comment": "Enriched with PS1 data on 2026-03-02",
  "add_params": [
    {
      "name": "tenantFilter",
      "in": "body",
      "required": true,
      "type": "string",
      "description": "Target tenant domain",
      "confidence": "high"
    },
    {
      "name": "BulkUser",
      "in": "body",
      "required": true,
      "type": "array",
      "description": "Array of user objects to create. Each element is a user row (typically from CSV import).",
      "confidence": "high"
    },
    {
      "name": "licenses",
      "in": "body",
      "required": false,
      "type": "array",
      "description": "License SKU IDs to assign to all created users. Accepts array of GUID strings or LabelValue objects.",
      "confidence": "high"
    },
    {
      "name": "usageLocation",
      "in": "body",
      "required": false,
      "type": "LabelValue",
      "description": "Usage location applied to all created users. Accepts LabelValue or raw ISO country code string.",
      "confidence": "high"
    },
    {
      "name": "value",
      "in": "body",
      "required": false,
      "type": "string",
      "description": "Body parameter from PS1: value"
    },
    {
      "name": "label",
      "in": "body",
      "required": false,
      "type": "string",
      "description": "Body parameter from PS1: label"
    }
  ],
  "override_synopsis": "Bulk-create users in a tenant via CSV import",
  "remove_params": [
    "label",
    "value"
  ],
  "notes": "BulkUser is an array of user row objects. Each row supports: mailNickName (required), domain (required), displayName OR givenName+surname (one required), password (optional, auto-generated if omitted), businessPhones (optional), plus any additional Graph user properties via PSObject passthrough (open schema). label and value params removed \u2014 they are LabelValue unwrap artifacts from $UsageLocation.label and $AssignedLicenses.value access, not top-level body fields."
}
