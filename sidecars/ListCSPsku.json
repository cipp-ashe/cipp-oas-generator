{
  "override_confidence": "high",
  "_comment": "Enriched with PS1 data on 2026-03-02",
  "parameters": [
    {
      "name": "tenantFilter",
      "in": "query",
      "required": false,
      "type": "string",
      "description": "Query parameter from PS1: tenantFilter"
    },
    {
      "name": "currentSkuOnly",
      "in": "query",
      "required": false,
      "type": "string",
      "description": "Query parameter from PS1: currentSkuOnly"
    }
  ],
  "trust_level": "reversed",
  "override_methods": [
    "GET"
  ],
  "notes": "ORPHAN \u2014 frontend calls /api/ListCSPsku (CippAddEditUser.jsx, add-subscription) but no PS1 exists. CSP SKU listing is frontend-only as of v10.1.x."
}
