{
  "trust_level": "reversed",
  "override_methods": ["GET"],
  "notes": "ORPHAN — frontend calls /api/ListExternalTenantInfo (CippTenantLookup.jsx) but no PS1 exists. External tenant lookup is frontend-only as of v10.1.x."
}
