{
  "override_confidence": "high",
  "_comment": "Enriched with PS1 data on 2026-03-02",
  "parameters": [
    {
      "name": "Endpoint",
      "in": "query",
      "required": false,
      "type": "string",
      "description": "Query parameter from PS1: Endpoint"
    }
  ],
  "trust_level": "reversed",
  "override_methods": [
    "GET"
  ],
  "notes": "PROBABLE 404 \u2014 frontend calls /api/ListGraphExplorerPresets (CIPPTableToptoolbar.js, 3 sites) but no Invoke-ListGraphExplorerPresets.ps1 exists. PS1 is Invoke-ExecGraphExplorerPreset in Tenant/Tools. Failure mode: graph explorer preset selector empty. Fix: rename PS1 or update frontend URL."
}
