{
  "trust_level": "reversed",
  "override_methods": ["GET"],
  "notes": "PROBABLE 404 — frontend calls /api/ListUsersAndGroups (CippFormUserAndGroupSelector.jsx) but no Invoke-ListUsersAndGroups.ps1 exists. PS1 is Invoke-ListDirectoryObjects in CIPP/Core. Failure mode: user+group selector shows empty results. Fix: rename PS1 or update frontend URL."
}
