{
  "trust_level": "reversed",
  "override_methods": ["POST"],
  "notes": "RENAMED — frontend calls /api/RemoveSafeAttachmentsFilter as a row action but the PS1 is Invoke-EditSafeAttachmentsFilter. Removal is performed via a PATCH/edit operation, not a separate delete endpoint."
}
