{
  "override_confidence": "high",
  "trust_level": "reversed",
  "override_methods": [
    "POST"
  ],
  "notes": "PROBABLE 404 \u2014 frontend POSTs to /api/EditTenantGroup (tenants/groups/add.js) but no Invoke-EditTenantGroup.ps1 exists. PS1 is Invoke-ExecTenantGroup in CIPP/Settings. Failure mode: tenant group add/edit form submission fails. Fix: rename PS1 or update frontend URL."
}
