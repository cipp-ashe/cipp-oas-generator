{
  "trust_level": "reversed",
  "override_methods": ["GET"],
  "deprecated": true,
  "notes": "REMOVED — HaveIBeenPwned breach lookup feature was removed from the CIPP API. Frontend still calls /api/ListBreachesAccount (tools/breachlookup/index.js) but no PS1 exists."
}
