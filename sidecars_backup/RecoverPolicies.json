{
  "trust_level": "reversed",
  "override_methods": ["POST"],
  "add_params": [
    {"name": "templateId",    "in": "body", "required": true,  "type": "string", "description": "Drift management template ID (from URL query param)"},
    {"name": "backupDateTime","in": "body", "required": true,  "type": "string", "format": "date-time", "description": "Point-in-time backup to restore from"},
    {"name": "policyIds",     "in": "body", "required": true,  "type": "array",  "description": "Array of policy IDs to recover from the selected backup"}
  ],
  "notes": "ORPHAN — frontend calls /api/RecoverPolicies (src/pages/tenant/manage/recover-policies.js) but no PS1 exists yet. The page has a comment 'replace with actual API call' confirming this is unimplemented. Body params reconstructed from the recoverApi.mutate() call in the page source."
}
