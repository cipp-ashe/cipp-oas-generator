{
  "override_confidence": "high",
  "trust_level": "reversed",
  "override_methods": [
    "POST"
  ],
  "notes": "RENAMED \u2014 frontend calls /api/RemoveAntiPhishingFilter as a row action but the PS1 is Invoke-EditAntiPhishingFilter. Removal is performed via a PATCH/edit operation, not a separate delete endpoint. The frontend URL does not match the PS1 name."
}
